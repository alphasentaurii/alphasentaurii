usermod -l starfox pi
usermod -m -d /home/starfox starfox

passwd
raspberry

sophieprime

# check starfox has root priv
sudo apt-get update





sudo apt-get update

sudo apt-get install chromium-browser -yes



increase swap space

swap space: small amount of memory which stores some inactive pages when the RAM is full

increase from 100MB to 2000MB

> sudo nano /etc/dphys-swapfile

change line below:
CONF_SWAPSIZE=100
to:
CONF_SWAPSIZE=2048

save close restart
